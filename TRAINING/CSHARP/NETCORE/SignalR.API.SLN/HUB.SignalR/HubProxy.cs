namespace HUB.SignalR
{
    public class HubProxy
    {
        //protected async Task SendToUserAsync(IClientProxy proxy, string fnName, string name, CustomerInfo message, string user)
        //{
        //    await SendProxyAsync(Clients.User(user), fnName, name, message);
        //}

        //protected async Task SendToGroupAsync(IClientProxy proxy, string fnName, string name, CustomerInfo message, string group)
        //{
        //    await SendProxyAsync(Clients.Group(group), fnName, name, message);
        //}

        //protected async Task SendToAllAsync(IClientProxy proxy, string fnName, string name, CustomerInfo message)
        //{
        //    await SendProxyAsync(Clients.All, fnName, name, message);
        //}

        //protected async Task SendToUsersAsync(IClientProxy proxy, string fnName, string name, CustomerInfo message, params string[] users)
        //{
        //    await SendProxyAsync(Clients.Users(users), fnName, name, message);
        //}

        //protected async Task SendToGroupsAsync(IClientProxy proxy, string fnName, string name, CustomerInfo message, params string[] groups)
        //{
        //    await SendProxyAsync(Clients.Groups(groups), fnName, name, message);
        //}

        //protected async Task SendAllExceptAsync(IClientProxy proxy, string fnName, string name, CustomerInfo message, params string[] users)
        //{
        //    await SendProxyAsync(Clients.AllExcept(users), fnName, name, message);
        //}
    }
}
